spring:
  application:
    name: Magia
  datasource:
    url: jdbc:h2:mem:magia
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console